# Context Construction Package 配置

# 滑动窗口大小
window_size: 20

# Token 估算方式: "simple" (简单估算) 或 "tiktoken" (精确计算)
token_count_method: "simple"

# 默认时间格式
time_format: "%Y年%m月%d日 %H:%M"

# 是否在构建时自动添加时间信息
auto_add_time: true