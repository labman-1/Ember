# Graph Process 配置

neo4j:
  uri: "bolt://localhost:7687"
  user: "neo4j"
  password: "password"
  database: "neo4j"

pool:
  max_connection_lifetime: 3600
  max_connection_pool_size: 50
  connection_timeout: 30
  connection_acquisition_timeout: 60

features:
  check_apoc: true  # 是否检查 APOC 插件