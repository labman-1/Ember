# DB Process Package 配置

# 数据库连接（优先从环境变量读取）
database:
  host: ${PG_HOST:localhost}
  port: ${PG_PORT:5432}
  user: ${PG_USER:postgres}
  password: ${PG_PASSWORD:}
  database: ${PG_DB:ember_db}

# 连接池配置
pool:
  min_size: 5
  max_size: 20
  timeout: 30
  idle_timeout: 300